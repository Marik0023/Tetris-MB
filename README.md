# Magic Tetris

Cleaned version of the project with only the files used by the current pages.

## Kept structure

```text
Tetris-MB-main-cleaned/
├── assets/
│   └── logo.webp
├── css/
│   ├── leaderboard.css
├── js/
│   ├── leaderboard.js
│   └── lb-page.js
├── game.js
├── index.html
├── leaderboard.html
├── style.css
└── README.md
```

## Notes

- `index.html` uses `style.css` and `game.js`
- `leaderboard.html` uses `css/style.css`, `css/leaderboard.css`, `js/leaderboard.js`, and `js/lb-page.js`
- Legacy / unused duplicate files were removed to reduce repo clutter
