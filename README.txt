# Magic Tetris — structured version

## Files
- `index.html` — page markup
- `styles/main.css` — all styles
- `scripts/app.js` — all game logic and result card export logic

## Run
Open `index.html` in a browser.

The game logic was not refactored, only split out of the original single-file HTML so behavior should remain the same.
